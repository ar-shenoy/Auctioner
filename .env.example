# =============================================================================
# ENVIRONMENT CONFIGURATION - DEPLOYMENT GUIDE
# =============================================================================
# This file documents all environment variables needed for frontend and backend.
# Copy this file to .env and customize for your environment.
#
# DO NOT hardcode localhost, ports, or origins in code.
# All configuration must come from environment variables.
# =============================================================================

# FRONTEND (Vite) Configuration
# =============================================================================

# VITE_API_BASE (REQUIRED)
# The backend API base URL. Must be absolute (http/https).
# 
# Examples:
#   Local dev:           http://localhost:8000/api/v1
#   Vercel frontend:     https://api.auctioner.com/api/v1
#   Self-hosted backend: https://api.mycompany.com/api/v1
#
# If not set, the app will fail with an error on startup.
VITE_API_BASE=http://localhost:8000/api/v1

# VITE_WS_BASE (Optional)
# WebSocket endpoint for real-time updates
# Examples:
#   Local dev:           ws://localhost:8000
#   Production:          wss://api.auctioner.com
VITE_WS_BASE=ws://localhost:8000

# GEMINI_API_KEY (Optional)
# Google Gemini API key for AI commentary generation
GEMINI_API_KEY=

# BACKEND (FastAPI) Configuration
# =============================================================================
# Set these in backend/.env or pass as environment variables

# SECRET_KEY (REQUIRED in production)
# JWT signing secret. Generate with: openssl rand -base64 32
SECRET_KEY=dev-secret-key-change-in-production

# DATABASE_URL (REQUIRED)
# PostgreSQL connection string
# Format: postgresql+asyncpg://user:password@host:port/database
# 
# Examples:
#   Local:       postgresql+asyncpg://admin:admin@localhost:5432/cricket_db
#   Docker:      postgresql+asyncpg://admin:admin@postgres:5432/cricket_db
#   Cloud:       postgresql+asyncpg://user:pass@db.amazonaws.com:5432/cricket_db
DATABASE_URL=postgresql+asyncpg://admin:admin@postgres:5432/cricket_db

# CORS_ORIGINS (REQUIRED)
# Comma-separated list of allowed frontend origins
# The backend will only accept requests from these origins
#
# Format: origin1,origin2,origin3 (no spaces)
#
# Examples:
#   Local dev only:
#     CORS_ORIGINS=http://localhost:3000,http://localhost:5173
#   
#   Multiple environments:
#     CORS_ORIGINS=http://localhost:3000,https://app.auctioner.com,https://app-staging.auctioner.com
#   
#   Vercel + custom backend:
#     CORS_ORIGINS=https://auctioner.vercel.app,https://api.auctioner.com
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# DEBUG (Optional)
# Set to true for verbose logging in development
DEBUG=false

# HOST (Optional)
# Server bind address. Use 0.0.0.0 for Docker/cloud deployments
HOST=0.0.0.0

# PORT (Optional)
# Server port
PORT=8000

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
#
# DEVELOPMENT (localhost):
#   - Frontend runs on http://localhost:3000 or http://localhost:5173
#   - Backend runs on http://localhost:8000
#   - Use local .env file with localhost values
#
# PRODUCTION (Vercel, AWS, etc):
#   - Set environment variables in hosting platform
#   - Frontend: Set VITE_API_BASE to your backend URL
#   - Backend: Set CORS_ORIGINS to include your frontend URL
#   - Backend: Set SECRET_KEY to a strong random value
#   - Backend: Use production database connection string
#
# DOCKER:
#   - Pass environment variables in docker run or docker-compose.yml
#   - Use network aliases: http://backend:8000/api/v1 inside Docker network
#
# CLOUD PLATFORMS:
#   - Vercel: Set VITE_API_BASE in Environment Variables
#   - Heroku: Set config vars via CLI or dashboard
#   - AWS: Set SSM Parameters or Lambda environment variables
#   - GCP: Set Cloud Run environment variables
#
# =============================================================================
