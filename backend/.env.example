# Backend Configuration (Development)
# For production, set these in your deployment platform (e.g., Render, Railway)

# Application
DEBUG=true
SECRET_KEY=dev-secret-key-change-in-production

# Database (PostgreSQL)
# Format: postgresql+asyncpg://user:password@host:port/dbname
DATABASE_URL=postgresql+asyncpg://admin:admin@postgres:5432/cricket_db

# CORS (Frontend Origins)
# Comma-separated list of allowed origins
# Dev: http://localhost:3000,http://localhost:5173
# Prod: https://your-frontend-app.vercel.app
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Host & Port
HOST=0.0.0.0
PORT=8000
